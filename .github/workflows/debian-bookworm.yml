---

name: Debian Bookworm

on:
  push:
    paths:
      - Dockerfile.debian-bookworm
      - .github/workflows/docker.yml
      - .github/workflows/debian-bookworm.yml
  pull_request:
    paths:
      - Dockerfile.debian-bookworm
      - .github/workflows/docker.yml
      - .github/workflows/debian-bookworm.yml

jobs:
  docker:
    uses: $/.github/workflows/docker.yml
    with:
      name: Debian Bookworm
      distro: debian-bookworm
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}

...
